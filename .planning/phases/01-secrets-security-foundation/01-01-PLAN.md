---
phase: 01-secrets-security-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/__init__.py
  - tests/test_sec01.py
  - tests/test_sec02.py
  - tests/test_sec03.py
  - requirements.txt
autonomous: true
requirements:
  - SEC-01
  - SEC-02
  - SEC-03

must_haves:
  truths:
    - "Running pytest tests/ -x -q -m 'not integration' exits 0 with all tests collected and passing (or correctly skipped)"
    - "Tests assert the absence of the plaintext password string in db_marks.py and db_connection.py"
    - "Tests assert .env and chat_memory.json appear in .gitignore"
    - "Tests assert bcrypt.hashpw/checkpw round-trip produces a hash starting with $2b$"
  artifacts:
    - path: "tests/__init__.py"
      provides: "Package marker so pytest collects the tests/ directory"
    - path: "tests/test_sec01.py"
      provides: "SEC-01 automated verification: env-loading pattern and no plaintext credential"
      exports: ["test_credentials_from_env", "test_db_connection_from_env", "test_no_plaintext_password"]
    - path: "tests/test_sec02.py"
      provides: "SEC-02 automated verification: .gitignore contains required entries"
      exports: ["test_env_in_gitignore", "test_chat_memory_in_gitignore"]
    - path: "tests/test_sec03.py"
      provides: "SEC-03 automated verification: bcrypt hash format and round-trip"
      exports: ["test_bcrypt_hash_format", "test_bcrypt_round_trip"]
  key_links:
    - from: "tests/test_sec01.py"
      to: "db_marks.py"
      via: "ast.parse or grep on source text"
      pattern: "no literal 'qCsfeuECc3MW' string"
    - from: "tests/test_sec02.py"
      to: ".gitignore"
      via: "open('.gitignore').read()"
      pattern: "'.env' in content and 'chat_memory.json' in content"
---

<objective>
Create the test scaffolding (Wave 0) that provides automated verify commands for all Phase 1 tasks. Tests must be written before implementation so later plans can run `pytest tests/ -x -q -m "not integration"` as their verify step.

Purpose: Nyquist compliance — every implementation task needs a runnable `<automated>` verify command. Without these test files the verify steps in Plans 02 and 03 have nothing to run.
Output: tests/ directory with four files covering SEC-01, SEC-02, and SEC-03 (unit/smoke only — no live DB required).
</objective>

<execution_context>
@/Users/kccsw/.claude/get-shit-done/workflows/execute-plan.md
@/Users/kccsw/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-secrets-security-foundation/01-CONTEXT.md
@.planning/phases/01-secrets-security-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add pytest to requirements.txt and create tests package</name>
  <files>requirements.txt, tests/__init__.py</files>
  <action>
    Append `pytest==8.3.5` to requirements.txt on a new line after the existing entries. The current requirements.txt has: streamlit, sentence-transformers, faiss-cpu, mysql-connector-python, pandas — no version pins yet. Add pytest with a pinned version.

    Then create `tests/__init__.py` as an empty file (just a newline). This makes `tests/` a Python package so pytest discovers it correctly.

    Do NOT change the existing unpinned entries in requirements.txt — version pinning of existing packages is an ARCH-03 task in Phase 4, not this phase.
  </action>
  <verify>
    <automated>python -c "import pathlib; lines = pathlib.Path('requirements.txt').read_text().splitlines(); assert any('pytest' in l for l in lines), 'pytest missing from requirements.txt'; assert pathlib.Path('tests/__init__.py').exists(), 'tests/__init__.py missing'; print('OK')"</automated>
  </verify>
  <done>requirements.txt contains a pytest line with version pin; tests/__init__.py exists as an empty file</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Write SEC-01 tests (env-loading and no plaintext credential)</name>
  <files>tests/test_sec01.py</files>
  <behavior>
    - test_credentials_from_env: Import db_marks module. Mock os.environ with fake DB credentials. Assert that get_connection() calls mysql.connector.connect with values taken from os.environ (not hardcoded strings). Implementation note: since db_marks.py currently HAS hardcoded credentials, this test should FAIL on the current source — it will go GREEN after Plan 02 patches db_marks.py.
    - test_db_connection_from_env: Same test targeting db_connection.py's get_connection() function.
    - test_no_plaintext_password: Read the source text of db_marks.py and db_connection.py as strings. Assert the literal string "qCsfeuECc3MW" does not appear in either file. This test FAILS now, passes after Plan 02 removes the hardcoded password.
  </behavior>
  <action>
    Create tests/test_sec01.py with three test functions.

    For test_credentials_from_env and test_db_connection_from_env: use unittest.mock.patch to replace mysql.connector.connect with a MagicMock, set os.environ values for DB_HOST/DB_USER/DB_PASSWORD/DB_NAME, call the function under test, assert mock was called with keyword arg password equal to the env var value (not the hardcoded string). Import the module INSIDE the test function (not at module level) after patching so load_dotenv() does not interfere.

    For test_no_plaintext_password: use pathlib.Path to read the file contents of db_marks.py and db_connection.py as strings. Assert "qCsfeuECc3MW" not in the content of each file. No mocking needed.

    All three tests should be plain pytest functions (no class wrapper needed). Do not use @pytest.mark.integration — these are unit/smoke tests that run without a live DB.
  </action>
  <verify>
    <automated>pytest tests/test_sec01.py::test_no_plaintext_password -x -q 2>&1 | head -5</automated>
  </verify>
  <done>tests/test_sec01.py exists with three test functions; test_no_plaintext_password is immediately runnable (will fail until Plan 02 patches the source files, which is expected and correct)</done>
</task>

<task type="auto">
  <name>Task 3: Write SEC-02 and SEC-03 tests</name>
  <files>tests/test_sec02.py, tests/test_sec03.py</files>
  <action>
    Create tests/test_sec02.py with two test functions:

    - test_env_in_gitignore: Read .gitignore (pathlib.Path('.gitignore').read_text()). Assert ".env" appears as a line entry. Use `any(line.strip() == '.env' for line in content.splitlines())` — not substring search, to avoid false positives from partial matches like 'venv/.env'.
    - test_chat_memory_in_gitignore: Same pattern. Assert "chat_memory.json" appears as a line entry.

    Both tests will FAIL until Plan 02 creates .gitignore. That is correct and expected.

    Create tests/test_sec03.py with two test functions:

    - test_bcrypt_hash_format: Import bcrypt. Hash the bytes b"test_password" using bcrypt.hashpw(b"test_password", bcrypt.gensalt()). Assert the result starts with b"$2b$". Assert len(hashed) == 60.
    - test_bcrypt_round_trip: Hash b"test_password". Call bcrypt.checkpw(b"test_password", hashed). Assert it returns True. Call bcrypt.checkpw(b"wrong_password", hashed). Assert it returns False.

    SEC-03 tests do NOT require a live database — they test the bcrypt library behavior in isolation. Do not mark them as integration tests.
  </action>
  <verify>
    <automated>pytest tests/test_sec03.py -x -q 2>&1 | head -10</automated>
  </verify>
  <done>tests/test_sec02.py and tests/test_sec03.py exist; test_sec03.py passes immediately (bcrypt library tests require only the bcrypt package); test_sec02.py fails until Plan 02 creates .gitignore (expected)</done>
</task>

</tasks>

<verification>
Run the full test collection to confirm pytest discovers all tests without import errors:

```
pytest tests/ --collect-only -q
```

Expected: 8 test items collected across test_sec01.py (3), test_sec02.py (2), test_sec03.py (2), plus the package init. No collection errors.

Run only the tests that should pass right now (bcrypt round-trip, which has no dependencies on implementation):

```
pytest tests/test_sec03.py -v
```

Expected: 2 tests pass.
</verification>

<success_criteria>
- `pytest tests/ --collect-only -q` collects 7 test items with no import errors
- `pytest tests/test_sec03.py -v` shows 2 passing tests
- `pytest tests/test_sec01.py::test_no_plaintext_password -x` runs (even if it fails — it must be runnable, not crash with ImportError)
- tests/__init__.py exists as an empty file
- requirements.txt contains a pytest line with version pin
</success_criteria>

<output>
After completion, create `.planning/phases/01-secrets-security-foundation/01-01-SUMMARY.md`
</output>
